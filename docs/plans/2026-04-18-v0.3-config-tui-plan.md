# v0.3.0 Config utilisateur + TUI — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ajouter une config utilisateur TOML `~/.config/ccfaststatus/config.toml` pour activer/désactiver chaque segment, avec une TUI ratatui plein-écran pour l'éditer. Le rendu sans config file reste strictement identique à v0.2.

**Architecture:** Nouveau module `src/settings.rs` pour parser/écrire le TOML, nouveau module `src/tui/` pour l'interface ratatui avec 3 panneaux (catégories / options / preview). Intégration dans `main.rs::render` par des `if` gardes sur chaque `segments.push`, et dans `install.rs::run` par un prompt optionnel post-install qui lance la TUI.

**Tech Stack:**
- Rust 2021 edition
- `toml` crate (nouveau, parsing)
- `ratatui` + `crossterm` crates (nouveaux, TUI plein-écran)
- Test convention existante : tests unitaires inline `#[cfg(test)] mod tests` + golden tests dans `tests/golden.rs`

**Design doc source :** `docs/plans/2026-04-18-roadmap-design.md`

**Branche :** commits directs sur `main` (convention projet). Chaque commit inclut `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.

**Principe non-négociable :** rétro-compat stricte. Tous les golden tests existants doivent passer sans modification à chaque commit.

---

## Phase A — Fondation Settings (sans TUI)

### Task A1 : Ajouter la dépendance `toml`

**Files:**
- Modify: `Cargo.toml`

**Step 1: Éditer Cargo.toml**

Ajouter dans le bloc `[dependencies]` :
```toml
toml = { version = "0.8", default-features = false, features = ["parse", "display"] }
```
Le `default-features = false` évite `serde` transitive (non nécessaire, on parse en `toml::Value`).

**Step 2: Vérifier la compilation**

Run: `cargo build --release`
Expected: compilation OK, pas d'erreur

**Step 3: Commit**

```bash
git add Cargo.toml Cargo.lock
git commit -m "$(cat <<'EOF'
build: add toml dependency for user config parsing

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task A2 : Squelette `src/settings.rs` avec types et défauts

**Files:**
- Create: `src/settings.rs`
- Modify: `src/main.rs` (ajouter `mod settings;`)

**Step 1: Écrire le premier test (TDD)**

Créer `src/settings.rs` avec uniquement :
```rust
#![allow(dead_code)]

#[derive(Debug, Clone, PartialEq)]
pub struct SegmentFlags {
    pub time: bool,
    pub model: bool,
    pub folder: bool,
    pub git: bool,
    pub context: bool,
    pub cost: bool,
    pub limits: bool,
    pub version: bool,
}

impl Default for SegmentFlags {
    fn default() -> Self {
        Self {
            time: true,
            model: true,
            folder: true,
            git: true,
            context: true,
            cost: true,
            limits: true,
            version: true,
        }
    }
}

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Settings {
    pub segments: SegmentFlags,
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn default_has_all_segments_enabled() {
        let s = Settings::default();
        assert!(s.segments.time);
        assert!(s.segments.model);
        assert!(s.segments.folder);
        assert!(s.segments.git);
        assert!(s.segments.context);
        assert!(s.segments.cost);
        assert!(s.segments.limits);
        assert!(s.segments.version);
    }
}
```

**Step 2: Ajouter le module dans main.rs**

Dans `src/main.rs`, après `mod install;` ajouter `mod settings;`.

**Step 3: Lancer les tests**

Run: `cargo test --bin ccfaststatus settings::`
Expected: `default_has_all_segments_enabled` passe.

**Step 4: Commit**

```bash
git add src/settings.rs src/main.rs
git commit -m "$(cat <<'EOF'
feat(settings): add SegmentFlags and Settings types with all-on defaults

Socle pour la config utilisateur. Default == comportement v0.2 inchangé.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task A3 : Résolution du chemin de config

**Files:**
- Modify: `src/settings.rs`

**Step 1: Écrire les tests d'abord**

Ajouter dans `mod tests` :
```rust
#[test]
fn config_path_uses_xdg_config_home_when_set() {
    let saved = std::env::var("XDG_CONFIG_HOME").ok();
    std::env::set_var("XDG_CONFIG_HOME", "/custom/xdg");
    let p = config_path().unwrap();
    assert_eq!(p, std::path::PathBuf::from("/custom/xdg/ccfaststatus/config.toml"));
    match saved {
        Some(v) => std::env::set_var("XDG_CONFIG_HOME", v),
        None => std::env::remove_var("XDG_CONFIG_HOME"),
    }
}

#[test]
fn config_path_falls_back_to_home_dot_config() {
    let saved_xdg = std::env::var("XDG_CONFIG_HOME").ok();
    let saved_home = std::env::var("HOME").ok();
    std::env::remove_var("XDG_CONFIG_HOME");
    std::env::set_var("HOME", "/tmp/fake-home");
    let p = config_path().unwrap();
    assert_eq!(p, std::path::PathBuf::from("/tmp/fake-home/.config/ccfaststatus/config.toml"));
    match saved_xdg { Some(v) => std::env::set_var("XDG_CONFIG_HOME", v), None => () }
    match saved_home { Some(v) => std::env::set_var("HOME", v), None => std::env::remove_var("HOME") }
}
```

Note : ces deux tests touchent `std::env` ; ils sont **non parallélisables**. Lancer avec `cargo test --bin ccfaststatus settings::config_path -- --test-threads=1`.

**Step 2: Lancer les tests, vérifier l'échec**

Run: `cargo test --bin ccfaststatus settings::config_path -- --test-threads=1`
Expected: FAIL (`config_path` n'existe pas).

**Step 3: Implémenter `config_path`**

Ajouter en haut du fichier (après les struct) :
```rust
use std::path::PathBuf;

pub fn config_path() -> Result<PathBuf, String> {
    if let Ok(xdg) = std::env::var("XDG_CONFIG_HOME") {
        if !xdg.is_empty() {
            return Ok(PathBuf::from(xdg).join("ccfaststatus").join("config.toml"));
        }
    }
    let home = std::env::var("HOME")
        .map_err(|_| "variable d'environnement HOME non définie".to_string())?;
    Ok(PathBuf::from(home).join(".config").join("ccfaststatus").join("config.toml"))
}
```

**Step 4: Relancer les tests**

Run: `cargo test --bin ccfaststatus settings::config_path -- --test-threads=1`
Expected: PASS.

**Step 5: Commit**

```bash
git add src/settings.rs
git commit -m "$(cat <<'EOF'
feat(settings): resolve XDG config path with HOME fallback

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task A4 : Parser TOML vers `Settings`

**Files:**
- Modify: `src/settings.rs`

**Step 1: Écrire les tests de parsing**

Ajouter :
```rust
#[test]
fn parse_empty_toml_returns_defaults() {
    let s = Settings::parse_toml("").unwrap();
    assert_eq!(s, Settings::default());
}

#[test]
fn parse_full_config() {
    let t = r#"
[segments]
time    = false
model   = true
folder  = false
git     = true
context = true
cost    = false
limits  = true
version = false
"#;
    let s = Settings::parse_toml(t).unwrap();
    assert!(!s.segments.time);
    assert!(s.segments.model);
    assert!(!s.segments.folder);
    assert!(s.segments.git);
    assert!(!s.segments.cost);
    assert!(!s.segments.version);
}

#[test]
fn parse_partial_config_keeps_defaults_for_missing_keys() {
    let t = r#"
[segments]
git = false
"#;
    let s = Settings::parse_toml(t).unwrap();
    assert!(!s.segments.git, "git explicitly disabled");
    assert!(s.segments.time, "time keeps default true");
    assert!(s.segments.model, "model keeps default true");
}

#[test]
fn parse_unknown_keys_are_ignored() {
    let t = r#"
[segments]
git = false
future_segment = true

[future_section]
key = "value"
"#;
    let s = Settings::parse_toml(t).unwrap();
    assert!(!s.segments.git);
    // Aucune panic, parsing tolérant.
}

#[test]
fn parse_malformed_toml_returns_err() {
    let t = "[[[ not valid";
    assert!(Settings::parse_toml(t).is_err());
}

#[test]
fn all_flags_false_forces_model_true() {
    let t = r#"
[segments]
time = false
model = false
folder = false
git = false
context = false
cost = false
limits = false
version = false
"#;
    let s = Settings::parse_toml(t).unwrap();
    assert!(s.segments.model, "model forcé à true si tout est désactivé");
}
```

**Step 2: Vérifier l'échec**

Run: `cargo test --bin ccfaststatus settings::parse`
Expected: FAIL.

**Step 3: Implémenter `parse_toml`**

Ajouter dans `impl Settings` :
```rust
impl Settings {
    pub fn parse_toml(s: &str) -> Result<Self, String> {
        let value: toml::Value = s.parse()
            .map_err(|e: toml::de::Error| format!("TOML invalide : {}", e))?;

        let mut settings = Settings::default();

        if let Some(segs) = value.get("segments").and_then(|v| v.as_table()) {
            let get = |k: &str, d: bool| segs.get(k).and_then(|v| v.as_bool()).unwrap_or(d);
            settings.segments = SegmentFlags {
                time:    get("time",    true),
                model:   get("model",   true),
                folder:  get("folder",  true),
                git:     get("git",     true),
                context: get("context", true),
                cost:    get("cost",    true),
                limits:  get("limits",  true),
                version: get("version", true),
            };
        }

        // Garde-fou : si tous les flags sont false, force model = true (sinon output vide)
        let f = &settings.segments;
        if !f.time && !f.model && !f.folder && !f.git
            && !f.context && !f.cost && !f.limits && !f.version
        {
            settings.segments.model = true;
        }

        Ok(settings)
    }
}
```

**Step 4: Tests**

Run: `cargo test --bin ccfaststatus settings::parse`
Expected: PASS (tous les tests ajoutés à l'étape 1).

**Step 5: Commit**

```bash
git add src/settings.rs
git commit -m "$(cat <<'EOF'
feat(settings): parse TOML config with partial/unknown-keys tolerance

- Clé absente → défaut (true).
- Clé inconnue → ignorée (forward-compat).
- Tous false → model forcé true (évite output vide).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task A5 : `Settings::load()` avec gestion fichier absent et malformé

**Files:**
- Modify: `src/settings.rs`

**Step 1: Écrire les tests**

Ajouter :
```rust
#[test]
fn load_missing_file_returns_defaults() {
    let tmp = std::env::temp_dir().join("ccfaststatus-test-missing.toml");
    let _ = std::fs::remove_file(&tmp);
    let s = Settings::load_from(&tmp);
    assert_eq!(s, Settings::default());
}

#[test]
fn load_valid_file_parses() {
    let tmp = std::env::temp_dir().join("ccfaststatus-test-valid.toml");
    std::fs::write(&tmp, "[segments]\ngit = false\n").unwrap();
    let s = Settings::load_from(&tmp);
    assert!(!s.segments.git);
    assert!(s.segments.time);
    let _ = std::fs::remove_file(&tmp);
}

#[test]
fn load_malformed_file_returns_defaults() {
    let tmp = std::env::temp_dir().join("ccfaststatus-test-bad.toml");
    std::fs::write(&tmp, "[[[ broken").unwrap();
    let s = Settings::load_from(&tmp);
    // Pas de panic, défauts appliqués.
    assert_eq!(s, Settings::default());
    let _ = std::fs::remove_file(&tmp);
}
```

**Step 2: Vérifier l'échec**

Run: `cargo test --bin ccfaststatus settings::load`
Expected: FAIL.

**Step 3: Implémenter**

Ajouter dans `impl Settings` :
```rust
pub fn load_from(path: &std::path::Path) -> Self {
    match std::fs::read_to_string(path) {
        Ok(content) => match Self::parse_toml(&content) {
            Ok(s) => s,
            Err(e) => {
                eprintln!("ccfaststatus: {} invalide, défauts appliqués : {}", path.display(), e);
                Settings::default()
            }
        },
        Err(e) if e.kind() == std::io::ErrorKind::NotFound => Settings::default(),
        Err(e) => {
            eprintln!("ccfaststatus: impossible de lire {} : {}", path.display(), e);
            Settings::default()
        }
    }
}

pub fn load() -> Self {
    match config_path() {
        Ok(p) => Self::load_from(&p),
        Err(_) => Settings::default(),
    }
}
```

**Step 4: Tests**

Run: `cargo test --bin ccfaststatus settings::load`
Expected: PASS.

**Step 5: Commit**

```bash
git add src/settings.rs
git commit -m "$(cat <<'EOF'
feat(settings): Settings::load() with graceful fallback

File missing → defaults. Malformed → stderr warning + defaults.
Jamais de panic sur le hot path.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task A6 : Écriture `Settings` vers TOML

**Files:**
- Modify: `src/settings.rs`

**Step 1: Tests**

```rust
#[test]
fn to_toml_roundtrip() {
    let mut s = Settings::default();
    s.segments.git = false;
    s.segments.version = false;
    let serialized = s.to_toml();
    let reparsed = Settings::parse_toml(&serialized).unwrap();
    assert_eq!(s, reparsed);
}

#[test]
fn write_roundtrip() {
    let tmp = std::env::temp_dir().join("ccfaststatus-test-write.toml");
    let mut s = Settings::default();
    s.segments.folder = false;
    s.save_to(&tmp).unwrap();
    let loaded = Settings::load_from(&tmp);
    assert_eq!(s, loaded);
    let _ = std::fs::remove_file(&tmp);
}

#[test]
fn write_creates_parent_dir() {
    let tmp = std::env::temp_dir().join("ccfaststatus-test-dir/config.toml");
    let _ = std::fs::remove_dir_all(tmp.parent().unwrap());
    Settings::default().save_to(&tmp).unwrap();
    assert!(tmp.exists());
    let _ = std::fs::remove_dir_all(tmp.parent().unwrap());
}
```

**Step 2: Vérifier l'échec**

Run: `cargo test --bin ccfaststatus settings::`
Expected: FAIL (to_toml/save_to n'existent pas).

**Step 3: Implémenter**

```rust
pub fn to_toml(&self) -> String {
    let f = &self.segments;
    format!(
r#"[segments]
time    = {}
model   = {}
folder  = {}
git     = {}
context = {}
cost    = {}
limits  = {}
version = {}
"#,
        f.time, f.model, f.folder, f.git,
        f.context, f.cost, f.limits, f.version,
    )
}

pub fn save_to(&self, path: &std::path::Path) -> Result<(), String> {
    if let Some(parent) = path.parent() {
        std::fs::create_dir_all(parent)
            .map_err(|e| format!("impossible de créer {} : {}", parent.display(), e))?;
    }
    std::fs::write(path, self.to_toml())
        .map_err(|e| format!("impossible d'écrire {} : {}", path.display(), e))
}
```

**Step 4: Tests**

Run: `cargo test --bin ccfaststatus settings::`
Expected: PASS.

**Step 5: Commit**

```bash
git add src/settings.rs
git commit -m "$(cat <<'EOF'
feat(settings): serialize Settings to TOML with directory auto-creation

Round-trip garanti via tests. Format lisible à la main.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task A7 : Intégrer `Settings::load()` dans `main.rs::render`

**Files:**
- Modify: `src/main.rs`

**Step 1: Vérifier l'état actuel**

Run: `cargo test` (tous les tests, y compris golden)
Expected: TOUS passent. C'est la baseline pré-intégration.

**Step 2: Modifier `main.rs::render`**

Dans `src/main.rs`, fonction `pub(crate) fn render(data: ClaudeInput, cols: usize) -> String` :

1. Au début de la fonction (après le commentaire `// 2. Resolve cwd`), ajouter :
```rust
let settings = settings::Settings::load();
let flags = &settings.segments;
```

2. Wrapper chaque `segments.push(...)` par son flag. Exemples précis :

- Après `// 4.0 Time segment` : wrapper `segments.push(Segment { text: time_text, bg: BG_TIME, priority: P_TIME });` dans `if flags.time { ... }`. Mais attention : il faut aussi ne pas construire `time_text` si le flag est `false`. Donc entourer tout le bloc `// 4.0 Time segment (...)` à partir de `let now = Local::now();` jusqu'au `segments.push(...)` inclus par :
```rust
if flags.time {
    // bloc existant
}
```

- Idem pour `// 4.1 Model segment` → `if flags.model { ... }`
- `// 4.2 Folder + worktree indicator` → `if flags.folder { ... }`
- `// 4.3 Git segment` → déjà `if git_info.is_repo { ... }`, imbriquer :
  ```rust
  if flags.git && git_info.is_repo { ... }
  ```
- `// 4.4 Context segment` → `if flags.context { ... }`
- `// 4.5 Rate limits OR cost` → wrapper :
  - `if data.rate_limits.is_some()` branche dans `if flags.limits && data.rate_limits.is_some() { ... }`
  - `else if cost_usd > 0.0` devient `if flags.cost && cost_usd > 0.0 { ... }` (branche séparée, plus de `else if` car maintenant les deux conditions sont indépendantes des flags)

  En fait le `else if` actuel dépend de `rate_limits.is_some()` — on doit préserver la sémantique "si limites disponibles, pas de cost". Rewrite :
  ```rust
  let show_limits = flags.limits && data.rate_limits.is_some();
  let show_cost = flags.cost && data.rate_limits.is_none() && cost_usd > 0.0;
  if show_limits {
      // bloc limites existant, sans le `else if`
      ...
  }
  if show_cost {
      // bloc cost existant
      ...
  }
  ```

- Version suffix (fin de `render`) : wrapper la construction de `version_suffix` par :
  ```rust
  let version_suffix = if flags.version && !data.version.is_empty() {
      // bloc existant
  } else {
      String::new()
  };
  ```

**Step 3: Vérifier que tous les golden tests passent**

Run: `cargo test`
Expected: TOUS passent (sans config file, defaults = tous true = comportement v0.2 inchangé).

Si un test échoue : debug, c'est qu'une garde est mal placée. **Interdit** de modifier un golden pour le faire passer.

**Step 4: Commit**

```bash
git add src/main.rs
git commit -m "$(cat <<'EOF'
feat: gate each segment behind Settings.segments flag

Defaults unchanged → golden tests pass untouched.
Préserve la mutex limits/cost (pas les deux en même temps).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task A8 : Golden test avec config `git=false`

**Files:**
- Create: `tests/fixtures/with_git.json` — déjà existant, ne pas modifier
- Create: `tests/fixtures/config_no_git.config.toml`
- Create: `tests/fixtures/config_no_git.expected`
- Modify: `tests/golden.rs` (ajouter un nouveau cas de test)

**Step 1: Lire `tests/golden.rs` pour comprendre le pattern actuel**

Run: lire le fichier existant (Read).
Comprendre comment les fixtures sont chargées (probablement `read_to_string` de la fixture .json + comparaison avec .expected).

**Step 2: Décider de l'API de test**

Le test doit pouvoir injecter une `Settings` différente. Deux options :
- (a) `render` prend `&Settings` en paramètre (refacto)
- (b) Exposer `render_with` dans `src/main.rs` qui prend une `Settings`, et `render` reste une fonction wrapper qui appelle `render_with(data, cols, Settings::load())`

Choisir (b) pour ne pas toucher le hot path ni la signature publique :
```rust
pub(crate) fn render(data: ClaudeInput, cols: usize) -> String {
    render_with(data, cols, settings::Settings::load())
}

pub(crate) fn render_with(data: ClaudeInput, cols: usize, settings: settings::Settings) -> String {
    let flags = &settings.segments;
    // ... reste du code existant, remplacer `Settings::load()` par l'argument
}
```

**Step 3: Exposer `render_with` pour les tests**

Dans `src/main.rs`, refactoriser selon l'étape précédente. Vérifier que tous les tests passent toujours :
Run: `cargo test`
Expected: PASS.

Commit intermédiaire :
```bash
git add src/main.rs
git commit -m "$(cat <<'EOF'
refactor: extract render_with(settings) for golden testing

Permet d'injecter une Settings custom dans les tests sans toucher à load().

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**Step 4: Ajouter le test golden avec config**

Dans `tests/golden.rs`, ajouter un test qui :
1. Lit `tests/fixtures/with_git.json` comme input
2. Construit une `Settings` avec `git = false`
3. Appelle `render_with(...)` (il faut exposer la fonction ou dupliquer la logique ; si `render_with` est `pub(crate)`, les tests d'intégration ne peuvent pas l'appeler directement. Soit promouvoir en `pub`, soit créer un helper dans un nouveau `src/lib.rs`)

**Décision simple** : promouvoir `render_with` en `pub` temporairement (documenté `#[doc(hidden)]`), le lien avec CLAUDE.md :
> `render()` est `pub(crate)` pour que `install::preview()` puisse l'appeler. Si un jour la crate devient `lib`, il faudra repasser en `pub`.

Donc on reste `pub(crate)` mais on ajoute `#[cfg(test)] pub use render_with;` ? Non, les tests d'intégration (`tests/*.rs`) ne voient pas les items `pub(crate)` du bin.

**Option pragmatique** : ajouter le test comme test unitaire dans `src/main.rs::tests` plutôt que dans `tests/golden.rs`. Créer la fixture dans `tests/fixtures/` est OK, l'accès via `include_str!` marche depuis `src/main.rs`.

Ajouter dans `src/main.rs::mod tests` :
```rust
#[test]
fn render_without_git_segment() {
    let json = include_str!("../tests/fixtures/with_git.json");
    let data: ClaudeInput = serde_json::from_str(json).unwrap();
    let mut settings = settings::Settings::default();
    settings.segments.git = false;
    let output = render_with(data, 200, settings);
    // Le segment git contient l'icône nf-md-source_branch U+F062C
    assert!(!output.contains("\u{F062C}"), "git segment should be absent");
    // Mais le reste (model, folder) est présent
    assert!(output.contains("\u{F06A9}"), "model segment should be present");
}
```

**Step 5: Tests**

Run: `cargo test --bin ccfaststatus render_without_git_segment`
Expected: PASS.

Run: `cargo test`
Expected: TOUT PASSE.

**Step 6: Commit**

```bash
git add src/main.rs
git commit -m "$(cat <<'EOF'
test: golden-style test for segment disable via Settings

Vérifie que flags.git = false retire effectivement l'icône git du rendu.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase B — TUI ratatui

### Task B1 : Ajouter `ratatui` et `crossterm`

**Files:**
- Modify: `Cargo.toml`

**Step 1: Éditer**

Dans `[dependencies]` :
```toml
ratatui = { version = "0.28", default-features = false, features = ["crossterm"] }
crossterm = "0.28"
```

**Step 2: Vérifier la compilation**

Run: `cargo build --release`
Expected: OK (nouveau binaire plus gros, ~2–3 Mo au lieu de ~1.5 Mo).

**Step 3: Commit**

```bash
git add Cargo.toml Cargo.lock
git commit -m "$(cat <<'EOF'
build: add ratatui + crossterm for interactive TUI config

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task B2 : Squelette `src/tui/mod.rs` avec state data-driven

**Files:**
- Create: `src/tui/mod.rs`
- Create: `src/tui/state.rs`
- Modify: `src/main.rs` (ajouter `mod tui;`)

**Step 1: Créer `src/tui/state.rs`**

```rust
use crate::settings::{SegmentFlags, Settings};

#[derive(Debug, Clone, Copy, PartialEq)]
pub enum Panel {
    Categories,
    Options,
}

#[derive(Debug, Clone, Copy, PartialEq)]
pub enum Category {
    Segments,
}

pub const ALL_CATEGORIES: &[Category] = &[Category::Segments];

/// Description d'un segment exposé dans la TUI. Data-driven :
/// ajouter un segment = ajouter une entrée ici, la TUI s'adapte.
pub struct SegmentDef {
    pub key: &'static str,
    pub label: &'static str,
    pub getter: fn(&SegmentFlags) -> bool,
    pub setter: fn(&mut SegmentFlags, bool),
}

pub const ALL_SEGMENTS: &[SegmentDef] = &[
    SegmentDef { key: "time",    label: "Horaire",           getter: |f| f.time,    setter: |f, v| f.time = v },
    SegmentDef { key: "model",   label: "Modèle",            getter: |f| f.model,   setter: |f, v| f.model = v },
    SegmentDef { key: "folder",  label: "Dossier",           getter: |f| f.folder,  setter: |f, v| f.folder = v },
    SegmentDef { key: "git",     label: "Git",               getter: |f| f.git,     setter: |f, v| f.git = v },
    SegmentDef { key: "context", label: "Contexte",          getter: |f| f.context, setter: |f, v| f.context = v },
    SegmentDef { key: "cost",    label: "Coût",              getter: |f| f.cost,    setter: |f, v| f.cost = v },
    SegmentDef { key: "limits",  label: "Rate limits (5h/7d)", getter: |f| f.limits,  setter: |f, v| f.limits = v },
    SegmentDef { key: "version", label: "Version",           getter: |f| f.version, setter: |f, v| f.version = v },
];

pub struct App {
    pub settings: Settings,
    pub focus: Panel,
    pub category_idx: usize,
    pub option_idx: usize,
    pub should_quit: bool,
    pub should_save: bool,
}

impl App {
    pub fn new(settings: Settings) -> Self {
        Self {
            settings,
            focus: Panel::Categories,
            category_idx: 0,
            option_idx: 0,
            should_quit: false,
            should_save: false,
        }
    }

    pub fn current_category(&self) -> Category {
        ALL_CATEGORIES[self.category_idx]
    }

    pub fn toggle_current_option(&mut self) {
        match self.current_category() {
            Category::Segments => {
                let def = &ALL_SEGMENTS[self.option_idx];
                let current = (def.getter)(&self.settings.segments);
                (def.setter)(&mut self.settings.segments, !current);
            }
        }
    }

    pub fn option_count(&self) -> usize {
        match self.current_category() {
            Category::Segments => ALL_SEGMENTS.len(),
        }
    }

    pub fn move_down(&mut self) {
        match self.focus {
            Panel::Categories => {
                if self.category_idx + 1 < ALL_CATEGORIES.len() {
                    self.category_idx += 1;
                    self.option_idx = 0;
                }
            }
            Panel::Options => {
                if self.option_idx + 1 < self.option_count() {
                    self.option_idx += 1;
                }
            }
        }
    }

    pub fn move_up(&mut self) {
        match self.focus {
            Panel::Categories => {
                if self.category_idx > 0 {
                    self.category_idx -= 1;
                    self.option_idx = 0;
                }
            }
            Panel::Options => {
                if self.option_idx > 0 {
                    self.option_idx -= 1;
                }
            }
        }
    }

    pub fn next_panel(&mut self) {
        self.focus = match self.focus {
            Panel::Categories => Panel::Options,
            Panel::Options => Panel::Categories,
        };
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn new_starts_with_defaults_and_no_quit() {
        let app = App::new(Settings::default());
        assert_eq!(app.focus, Panel::Categories);
        assert_eq!(app.category_idx, 0);
        assert_eq!(app.option_idx, 0);
        assert!(!app.should_quit);
        assert!(!app.should_save);
    }

    #[test]
    fn toggle_flips_current_segment() {
        let mut app = App::new(Settings::default());
        app.focus = Panel::Options;
        app.option_idx = 3;  // git
        assert!(app.settings.segments.git);
        app.toggle_current_option();
        assert!(!app.settings.segments.git);
        app.toggle_current_option();
        assert!(app.settings.segments.git);
    }

    #[test]
    fn move_down_in_options_is_bounded() {
        let mut app = App::new(Settings::default());
        app.focus = Panel::Options;
        for _ in 0..50 {
            app.move_down();
        }
        assert_eq!(app.option_idx, ALL_SEGMENTS.len() - 1);
    }

    #[test]
    fn next_panel_toggles() {
        let mut app = App::new(Settings::default());
        assert_eq!(app.focus, Panel::Categories);
        app.next_panel();
        assert_eq!(app.focus, Panel::Options);
        app.next_panel();
        assert_eq!(app.focus, Panel::Categories);
    }
}
```

**Step 2: Créer `src/tui/mod.rs` minimaliste**

```rust
pub mod state;

pub fn run(initial: crate::settings::Settings) -> Result<Option<crate::settings::Settings>, String> {
    // Stub : sera rempli en B3.
    let _ = initial;
    Err("non implémenté".to_string())
}
```

**Step 3: Déclarer `mod tui;` dans `src/main.rs`**

Dans les `mod ...;` en haut, ajouter `mod tui;`.

**Step 4: Tests**

Run: `cargo test --bin ccfaststatus tui::state::tests`
Expected: PASS (4 tests unitaires).

Run: `cargo build`
Expected: OK (pas d'erreur même si `run` est stub).

**Step 5: Commit**

```bash
git add src/tui/ src/main.rs
git commit -m "$(cat <<'EOF'
feat(tui): state model with data-driven segment list

ALL_SEGMENTS est la source de vérité : ajouter une métrique en v0.5
= ajouter une entrée, la TUI s'adapte automatiquement.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task B3 : Event loop et key bindings

**Files:**
- Create: `src/tui/events.rs`
- Modify: `src/tui/mod.rs`

**Step 1: Écrire les tests de dispatch clavier**

Créer `src/tui/events.rs` :
```rust
use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
use super::state::App;

/// Dispatch d'un KeyEvent vers l'état. Séparé pour permettre les tests sans IO terminal.
pub fn handle_key(app: &mut App, key: KeyEvent) {
    match (key.code, key.modifiers) {
        (KeyCode::Char('q'), _) | (KeyCode::Esc, _) => {
            app.should_quit = true;
        }
        (KeyCode::Char('s'), _) => {
            app.should_save = true;
            app.should_quit = true;
        }
        (KeyCode::Tab, _) | (KeyCode::BackTab, _) => app.next_panel(),
        (KeyCode::Down, _) | (KeyCode::Char('j'), _) => app.move_down(),
        (KeyCode::Up, _) | (KeyCode::Char('k'), _) => app.move_up(),
        (KeyCode::Char(' '), _) | (KeyCode::Enter, _) => app.toggle_current_option(),
        _ => {}
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use super::super::state::{App, Panel};
    use crate::settings::Settings;

    fn key(code: KeyCode) -> KeyEvent {
        KeyEvent::new(code, KeyModifiers::NONE)
    }

    #[test]
    fn q_quits_without_save() {
        let mut app = App::new(Settings::default());
        handle_key(&mut app, key(KeyCode::Char('q')));
        assert!(app.should_quit);
        assert!(!app.should_save);
    }

    #[test]
    fn s_saves_and_quits() {
        let mut app = App::new(Settings::default());
        handle_key(&mut app, key(KeyCode::Char('s')));
        assert!(app.should_quit);
        assert!(app.should_save);
    }

    #[test]
    fn space_toggles_in_options_panel() {
        let mut app = App::new(Settings::default());
        app.focus = Panel::Options;
        app.option_idx = 3; // git
        assert!(app.settings.segments.git);
        handle_key(&mut app, key(KeyCode::Char(' ')));
        assert!(!app.settings.segments.git);
    }

    #[test]
    fn tab_switches_panel() {
        let mut app = App::new(Settings::default());
        handle_key(&mut app, key(KeyCode::Tab));
        assert_eq!(app.focus, Panel::Options);
    }
}
```

**Step 2: Exposer dans `mod.rs`**

Dans `src/tui/mod.rs`, ajouter `pub mod events;`.

**Step 3: Vérifier**

Run: `cargo test --bin ccfaststatus tui::events`
Expected: PASS (4 tests).

**Step 4: Commit**

```bash
git add src/tui/
git commit -m "$(cat <<'EOF'
feat(tui): keyboard event dispatcher (testable without terminal)

q/Esc → quit, s → save, Tab → swap panel, Space/Enter → toggle, ↑↓/jk → nav.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task B4 : Layout et rendu ratatui (3 panneaux)

**Files:**
- Create: `src/tui/ui.rs`
- Modify: `src/tui/mod.rs`

**Step 1: Rédiger le rendu**

Créer `src/tui/ui.rs` :
```rust
use ratatui::{
    layout::{Constraint, Direction, Layout, Rect},
    style::{Modifier, Style, Stylize},
    text::Line,
    widgets::{Block, Borders, List, ListItem, Paragraph, Wrap},
    Frame,
};

use super::state::{App, Category, Panel, ALL_CATEGORIES, ALL_SEGMENTS};
use crate::input::ClaudeInput;

pub fn draw(f: &mut Frame, app: &App) {
    let size = f.area();

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Length(20),      // Catégories
            Constraint::Min(30),         // Options
            Constraint::Percentage(40),  // Preview
        ])
        .split(size);

    draw_categories(f, chunks[0], app);
    draw_options(f, chunks[1], app);
    draw_preview(f, chunks[2], app);
}

fn draw_categories(f: &mut Frame, area: Rect, app: &App) {
    let items: Vec<ListItem> = ALL_CATEGORIES
        .iter()
        .enumerate()
        .map(|(i, cat)| {
            let label = match cat {
                Category::Segments => "Segments",
            };
            let mut line = Line::from(label.to_string());
            if i == app.category_idx {
                line = line.style(Style::default().add_modifier(Modifier::REVERSED));
            }
            ListItem::new(line)
        })
        .collect();

    let title = if app.focus == Panel::Categories { "[Catégories]" } else { "Catégories" };
    let block = Block::default().title(title).borders(Borders::ALL);
    let list = List::new(items).block(block);
    f.render_widget(list, area);
}

fn draw_options(f: &mut Frame, area: Rect, app: &App) {
    let items: Vec<ListItem> = match app.current_category() {
        Category::Segments => ALL_SEGMENTS
            .iter()
            .enumerate()
            .map(|(i, def)| {
                let checked = (def.getter)(&app.settings.segments);
                let mark = if checked { "◉" } else { "◯" };
                let text = format!(" {} {}", mark, def.label);
                let mut line = Line::from(text);
                if app.focus == Panel::Options && i == app.option_idx {
                    line = line.style(Style::default().add_modifier(Modifier::REVERSED));
                }
                ListItem::new(line)
            })
            .collect(),
    };

    let title = if app.focus == Panel::Options { "[Options]" } else { "Options" };
    let block = Block::default().title(title).borders(Borders::ALL);
    let list = List::new(items).block(block);
    f.render_widget(list, area);
}

fn draw_preview(f: &mut Frame, area: Rect, app: &App) {
    let data: ClaudeInput = serde_json::from_str("{}").expect("{} valid");
    let inner_width = area.width.saturating_sub(2) as usize;
    let rendered = crate::render_with(data, inner_width, app.settings.clone());

    let help = "\n\nTab: panneau  ↑↓: naviguer  Espace: toggle  s: sauver  q: quitter";

    let text = format!("{}\n{}", rendered, help);
    let p = Paragraph::new(text)
        .wrap(Wrap { trim: false })
        .block(Block::default().title("Aperçu").borders(Borders::ALL));
    f.render_widget(p, area);
}
```

**Note importante :** `ratatui::widgets::Paragraph` n'affiche pas par défaut les séquences ANSI. Pour que la preview affiche les vraies couleurs, il faut utiliser `ansi_to_tui` (crate externe) **OU** une approche plus simple : rendre une version "lisible" de la statusline dans la preview (sans ANSI, juste le texte). **Décision v0.3** : preview simple en texte brut (pas de couleurs), à améliorer en v0.4 quand les thèmes arriveront et que la question deviendra critique.

Rewrite de `draw_preview` :
```rust
fn draw_preview(f: &mut Frame, area: Rect, app: &App) {
    let data: ClaudeInput = serde_json::from_str("{}").expect("{} valid");
    let inner_width = area.width.saturating_sub(2) as usize;
    let rendered_ansi = crate::render_with(data, inner_width, app.settings.clone());
    let plain = crate::term::strip_ansi(&rendered_ansi);

    let help = "\n\nTab: panneau  ↑↓: naviguer  Espace: toggle  s: sauver  q: quitter";
    let text = format!("{}\n{}", plain, help);
    let p = Paragraph::new(text)
        .wrap(Wrap { trim: false })
        .block(Block::default().title("Aperçu").borders(Borders::ALL));
    f.render_widget(p, area);
}
```

**Step 2: Déclarer dans `mod.rs`**

Dans `src/tui/mod.rs` ajouter `pub mod ui;`.

**Step 3: Vérifier la compilation**

Run: `cargo build`
Expected: OK.

**Step 4: Commit**

```bash
git add src/tui/
git commit -m "$(cat <<'EOF'
feat(tui): 3-panel layout (categories / options / preview)

Preview en texte brut pour v0.3 (strip_ansi). L'affichage ANSI coloré
dans la preview est reporté à v0.4 où les thèmes rendront la question
significative.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task B5 : Boucle principale `tui::run`

**Files:**
- Modify: `src/tui/mod.rs`

**Step 1: Implémenter la boucle**

Remplacer le stub `run` par :
```rust
pub mod events;
pub mod state;
pub mod ui;

use crossterm::{
    event::{self, DisableMouseCapture, EnableMouseCapture, Event},
    execute,
    terminal::{disable_raw_mode, enable_raw_mode, EnterAlternateScreen, LeaveAlternateScreen},
};
use ratatui::{backend::CrosstermBackend, Terminal};
use std::io;
use std::time::Duration;

use crate::settings::Settings;
use state::App;

pub fn run(initial: Settings) -> Result<Option<Settings>, String> {
    enable_raw_mode().map_err(|e| format!("raw mode: {}", e))?;
    let mut stdout = io::stdout();
    execute!(stdout, EnterAlternateScreen, EnableMouseCapture)
        .map_err(|e| format!("alt screen: {}", e))?;
    let backend = CrosstermBackend::new(stdout);
    let mut terminal = Terminal::new(backend).map_err(|e| format!("terminal init: {}", e))?;

    let mut app = App::new(initial);

    let result = main_loop(&mut terminal, &mut app);

    // Cleanup toujours exécuté (même en cas d'erreur).
    disable_raw_mode().ok();
    execute!(terminal.backend_mut(), LeaveAlternateScreen, DisableMouseCapture).ok();
    terminal.show_cursor().ok();

    result?;

    if app.should_save {
        Ok(Some(app.settings))
    } else {
        Ok(None)
    }
}

fn main_loop<B: ratatui::backend::Backend>(
    terminal: &mut Terminal<B>,
    app: &mut App,
) -> Result<(), String> {
    loop {
        terminal.draw(|f| ui::draw(f, app)).map_err(|e| format!("draw: {}", e))?;

        if event::poll(Duration::from_millis(100)).map_err(|e| format!("poll: {}", e))? {
            if let Event::Key(key) = event::read().map_err(|e| format!("read: {}", e))? {
                events::handle_key(app, key);
            }
        }

        if app.should_quit {
            return Ok(());
        }
    }
}
```

**Step 2: Compilation**

Run: `cargo build`
Expected: OK.

**Step 3: Test manuel rapide**

Run: `cargo run --bin ccfaststatus` dans un terminal (TTY)
Dans la prompt d'interval, appuyer Enter pour défaut.
Puis (si le prompt config est déjà ajouté) ou temporairement ajouter un appel à `tui::run(Settings::default())` dans `install.rs` pour tester visuellement.
**Test fonctionnel** : navigation Tab, ↑↓, Space toggle, preview se met à jour, `s` sauve, `q` quitte.

Si on n'a pas encore intégré le prompt (Task B6), ce test manuel sera fait à la Task B6.

**Step 4: Commit**

```bash
git add src/tui/mod.rs
git commit -m "$(cat <<'EOF'
feat(tui): main event loop with alt screen + cleanup on exit

Return Some(Settings) si sauvé, None sinon. Cleanup terminal toujours exécuté.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task B6 : Intégration dans `install.rs::run`

**Files:**
- Modify: `src/install.rs`

**Step 1: Lire les tests existants**

Les tests dans `install.rs` ne lancent pas `run()` directement, ils testent les helpers (`update_settings`, `parse_interval`, etc.). On peut ajouter une intégration dans `run_inner` sans casser de test.

**Step 2: Modifier `run_inner`**

À la fin de la fonction, après le bloc `preview()`, ajouter :

```rust
println!();
let configure = prompt_yes_no("Configurer ccfaststatus (segments visibles) ?", false);
if configure {
    let initial = crate::settings::Settings::load();
    match crate::tui::run(initial) {
        Ok(Some(new_settings)) => {
            let path = crate::settings::config_path()?;
            new_settings.save_to(&path)?;
            println!("Configuration sauvegardée dans {}", path.display());
        }
        Ok(None) => {
            println!("Configuration annulée.");
        }
        Err(e) => {
            eprintln!("Erreur TUI : {}", e);
        }
    }
}
```

Et ajouter `prompt_yes_no` :
```rust
fn prompt_yes_no(question: &str, default: bool) -> bool {
    use std::io::{stdin, stdout, BufRead, Write};
    let suffix = if default { "[O/n]" } else { "[o/N]" };
    print!("{} {} ", question, suffix);
    let _ = stdout().flush();
    let mut line = String::new();
    if stdin().lock().read_line(&mut line).is_err() {
        return default;
    }
    match line.trim().to_lowercase().as_str() {
        "" => default,
        "o" | "oui" | "y" | "yes" => true,
        _ => false,
    }
}
```

**Step 3: Tests (unitaires de `prompt_yes_no` via parse)**

Ajouter un test si possible — ici `prompt_yes_no` lit stdin, dur à tester sans refactor. Extraire la logique pure :
```rust
pub fn parse_yes_no(input: &str, default: bool) -> bool {
    match input.trim().to_lowercase().as_str() {
        "" => default,
        "o" | "oui" | "y" | "yes" => true,
        _ => false,
    }
}

fn prompt_yes_no(question: &str, default: bool) -> bool {
    use std::io::{stdin, stdout, BufRead, Write};
    let suffix = if default { "[O/n]" } else { "[o/N]" };
    print!("{} {} ", question, suffix);
    let _ = stdout().flush();
    let mut line = String::new();
    if stdin().lock().read_line(&mut line).is_err() {
        return default;
    }
    parse_yes_no(&line, default)
}
```

Puis tests :
```rust
#[test]
fn parse_yes_no_empty_returns_default() {
    assert!(parse_yes_no("", true));
    assert!(!parse_yes_no("", false));
    assert!(parse_yes_no("\n", true));
}

#[test]
fn parse_yes_no_accepts_variants() {
    assert!(parse_yes_no("o", false));
    assert!(parse_yes_no("oui", false));
    assert!(parse_yes_no("y", false));
    assert!(parse_yes_no("Yes", false));
}

#[test]
fn parse_yes_no_anything_else_is_no() {
    assert!(!parse_yes_no("n", true));
    assert!(!parse_yes_no("pfft", true));
    assert!(!parse_yes_no("42", true));
}
```

**Step 4: Tests**

Run: `cargo test --bin ccfaststatus install::`
Expected: PASS (anciens + 3 nouveaux).

**Step 5: Test manuel end-to-end**

Run: `cargo run --release --bin ccfaststatus` dans un terminal TTY.
Scenario :
1. L'install Claude Code s'affiche (déjà configuré ou à installer).
2. Prompt "Configurer ccfaststatus ? [o/N]" → répondre `o`.
3. TUI s'affiche. Naviguer avec Tab/↑↓, toggle quelques segments.
4. Preview se met à jour.
5. Appuyer `s` → retour au shell, message "Configuration sauvegardée...".
6. `cat ~/.config/ccfaststatus/config.toml` → contenu attendu avec les flags modifiés.
7. Re-lancer `ccfaststatus < some.json` en pipe : le rendu reflète la config.

**Step 6: Commit**

```bash
git add src/install.rs
git commit -m "$(cat <<'EOF'
feat(install): optional TUI config after Claude Code settings install

Ajoute un prompt post-install pour lancer la TUI ratatui et sauver
la config dans ~/.config/ccfaststatus/config.toml.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase C — Finitions

### Task C1 : README + CLAUDE.md

**Files:**
- Modify: `README.md`
- Modify: `CLAUDE.md`

**Step 1: Lire les deux fichiers**

Run: (Read) sur README.md et CLAUDE.md pour se caler sur le ton actuel.

**Step 2: README — ajouter section `## Configuration`**

Ajouter après la section existante d'install :

```markdown
## Configuration

ccfaststatus lit `~/.config/ccfaststatus/config.toml` (ou `$XDG_CONFIG_HOME/ccfaststatus/config.toml`) à chaque rendu. Sans fichier, tous les segments sont affichés (comportement par défaut).

### TUI interactive

Lance `ccfaststatus` dans un terminal (pas en pipe) puis accepte le prompt « Configurer ccfaststatus ? ». Utilise `Tab`/`↑↓`/`Espace` pour naviguer et toggler, `s` pour sauvegarder, `q` pour quitter.

### Format TOML manuel

```toml
[segments]
time    = true
model   = true
folder  = true
git     = true
context = true
cost    = true
limits  = true
version = true
```

Une clé absente conserve sa valeur par défaut (`true`). Si tous les segments sont à `false`, `model` est forcé à `true` pour éviter un rendu vide.
```

**Step 3: CLAUDE.md — mettre à jour l'architecture**

Dans la section "Architecture rapide", ajouter les nouveaux modules :
```
  settings.rs   -- config utilisateur TOML : Settings, SegmentFlags, load/save,
                   config_path (XDG + HOME fallback), garde-fou all-false→model=true
  tui/
    mod.rs      -- event loop ratatui + alt screen, retourne Option<Settings>
    state.rs    -- App + ALL_SEGMENTS (data-driven pour v0.5)
    events.rs   -- handle_key (testable sans terminal)
    ui.rs       -- draw : 3 panneaux (categories / options / preview)
```

Dans la section "Points d'attention", ajouter :
```
- `settings::Settings::load()` ne panique jamais : fichier absent ou
  TOML malformé → défauts + warning stderr. Cohérent avec le hot path.
- `ALL_SEGMENTS` dans `tui::state` est la source de vérité data-driven :
  ajouter un segment en v0.5 = ajouter une entrée, la TUI s'adapte.
- Preview TUI v0.3 : texte brut (strip_ansi). L'affichage ANSI coloré
  en preview est reporté à v0.4 avec les thèmes.
```

**Step 4: Commit**

```bash
git add README.md CLAUDE.md
git commit -m "$(cat <<'EOF'
docs: user config + TUI (v0.3) in README and CLAUDE.md

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task C2 : Bench de sanity sur le hot path

**Files:**
- N/A (bench ad-hoc)

**Step 1: Préparer un JSON d'entrée**

```bash
cat tests/fixtures/with_git.json | cargo run --release --bin ccfaststatus > /dev/null
```

**Step 2: Benchmark avec hyperfine**

```bash
cargo build --release
hyperfine --warmup 10 --runs 200 \
  'cat tests/fixtures/with_git.json | ./target/release/ccfaststatus'
```

Expected: mean < 10 ms (cible projet). Si > 10 ms :
1. Profiler avec `cargo instruments -t time --bin ccfaststatus` ou `samply` pour identifier la cause (probable : TOML parse à chaque invocation)
2. Si le coût vient du parsing TOML, envisager un cache binaire comme pour git

**Step 3: Si tout OK, pas de commit nécessaire (pas de changement)**

Sinon, profiler puis optimiser (peut devenir une tâche dédiée).

---

### Task C3 : Release v0.3.0 via `/publish`

**Files:**
- Modify: `Cargo.toml` (bump version via skill)

**Step 1: Vérifier que tout est committé**

Run: `git status`
Expected: clean

**Step 2: Lancer la skill `/publish`**

Le skill gère :
1. Bump `Cargo.toml` version 0.2.0 → 0.3.0
2. Commit + tag
3. Push (main + tag)
4. Attente CI GitHub Actions pour le build binaire
5. Récupération SHA256 du release asset
6. Mise à jour `Formula/ccfaststatus.rb` dans `r9r-dev/homebrew-tap`

**Step 3: Validation finale**

Une fois `brew update && brew upgrade ccfaststatus` effectué sur une machine de test :
1. Sans `~/.config/ccfaststatus/config.toml` : le rendu doit être identique à v0.2 (rétro-compat stricte).
2. `ccfaststatus` en TTY → prompt + TUI fonctionnelle.
3. Config sauvée → nouveau rendu en pipe reflète la config.

---

## Ordre d'exécution récapitulatif

| # | Task | Durée estimée | Commit |
|---|---|---|---|
| A1 | Dep `toml` | 3 min | 1 |
| A2 | Types Settings | 5 min | 1 |
| A3 | `config_path` | 10 min | 1 |
| A4 | `parse_toml` | 20 min | 1 |
| A5 | `load_from` / `load` | 10 min | 1 |
| A6 | `to_toml` / `save_to` | 10 min | 1 |
| A7 | Intégration dans render | 20 min | 1 |
| A8 | Golden test disable-git | 15 min | 2 (refacto + test) |
| B1 | Deps ratatui | 3 min | 1 |
| B2 | State data-driven | 20 min | 1 |
| B3 | Event dispatcher | 15 min | 1 |
| B4 | UI 3 panneaux | 25 min | 1 |
| B5 | Main loop | 15 min | 1 |
| B6 | Prompt install + test manuel | 20 min | 1 |
| C1 | Docs | 15 min | 1 |
| C2 | Bench hot path | 10 min | 0 (ou 1 si optim) |
| C3 | Release /publish | 15 min (+ CI async) | 1 (bump) |

Total : ~3 h de travail concentré, ~16 commits.

## Skills référencées

- @superpowers:executing-plans — pour suivre ce plan task-by-task
- @superpowers:test-driven-development — chaque task Phase A/B suit red-green-refactor
- @superpowers:verification-before-completion — avant chaque commit, vérifier que la test suite passe intégralement
- @publish — skill projet pour la release v0.3.0 (étape C3)
